This method is suitable for tasks with small sample sizes as the preprocessing complexity increases rapidly with sample size


The data are stored in the Data folder test_rd_parser for the test set and train_rd_parser for the test set. The format is Sequence\t Label\n

1 Go to getBiTriGramPCAlike_test Run the script sh CUBT_autoGetID.sh .
2 Run the LR_model folder and run the script sh CUBT_autoTrainPb_PCAlike.sh


The results are viewed in the log_log.
